[pytest]
pythonpath = .
testpaths = app/tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    integration: hits external APIs (set GSTIN_RUN_LIVE_TEST=1 where applicable)
